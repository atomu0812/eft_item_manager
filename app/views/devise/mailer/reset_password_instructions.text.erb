<%= @resource.email %> 様

パスワード再設定の申請を受け付けました。

以下のリンクから、新しいパスワードを設定してください。

<%= edit_password_url(@resource, reset_password_token: @token) %>

このメールに心当たりがない場合は、そのまま破棄してください。

上記リンクにアクセスして新しいパスワードを設定するまで、現在のパスワードは変更されません。